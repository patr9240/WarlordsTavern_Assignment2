@using WarlordsTavern.Models
@model FoodType

@helper Truncate(string input, int length)
{
if (input.Length <= length)
{
        @input
}
else
{
        @input.Substring(0, length)<text>...</text>
}
}

@{
    ViewBag.Title = "Browse";
}

<h2>Browsing Food Type: @Model.Name</h2>

<ul>
    @foreach (Food food in Model.Foods)
    {
        <li>
            @Html.ActionLink(food.Name,
           "Details", new { id = food.FoodID })
        </li>
    }
</ul>
<!--

<table class="table table-condensed ">
    <tr>
        <th>
            Dish Type
        </th>
        <th>
            Dish
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            Price
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FoodType.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Truncate(item.Description, 35)
            </td>
            <td>
                $@Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                <a href="@Url.Action("Details", new { id = item.FoodID })" class="btn btn-primary" title="Info"><i class="fa fa-info"></i></a>
            </td>
        </tr>
    }

</table>
    -->